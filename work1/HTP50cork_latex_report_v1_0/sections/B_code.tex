\chapter{(부록 B) 코드 발췌}

\section{HeatSolver 핵심 로직(발췌)}
본 부록은 보고서 이해를 위해 핵심 로직만 발췌/요약한 것이다.
전체 구현은 \texttt{code/app.py}를 참고한다.

\lstinputlisting{code/heat_solver_excerpt.py}

\section{GUI 스레딩/진행률 흐름(발췌)}
\lstinputlisting{code/gui_thread_excerpt.py}
